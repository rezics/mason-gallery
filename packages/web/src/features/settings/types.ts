import type {
  GalleryFeatureSettings,
  SettingsStorage,
} from "@mason-gallery/core";

export type WebSettings = GalleryFeatureSettings;

export type WebSettingsStorage = SettingsStorage<WebSettings>;
